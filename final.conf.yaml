---
- defaults:
    link:
      create: true
      relink: true

- shell:
  - [source ~/.config_env,Setting up environment]
  - [source ~/.zshrc,Sourcing ZSH config]
  - [chmod 600 ~/.ssh/authorized_keys,Setting mode for SSH keys]
